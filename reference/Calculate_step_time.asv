function step_time = Calculate_step_time()
    X = out.X_and_V(:,1);
    Xr = out.Xr;
    time = out.tout;

    Xr_max = max(Xr);
    index = find( X==0.9*Xr_max );%找到正好达到90%阈值时的下标
    step_time = time(index);        
end

